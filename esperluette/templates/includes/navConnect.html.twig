<div class="row navbar-fixed" id="navConnect">
    <!-- Dropdown Structure -->
    <ul id="dropdown1" class="dropdown-content">
        <li>
            <a href="{{ path('security_show') }}">
                Mon profil
            </a>
        </li>
        <li class="divider"></li>
        <li>
            <a href="{{ path('security_logout') }}">
                <i class="fas fa-sign-in-alt fa-sm"></i> Déconnexion
            </a>
        </li>
    </ul>
    <nav class="navigation">
        <div class="nav-wrapper">
            <ul class="navLogo left">
                <a href="{{ path('home') }}">
                    <img src="{{ asset('/images/logo.png') }}" alt="logo esperluette">
                </a>
            </ul>
            <ul class="right">
                <li>
                    <a href="{{ path('cart_index') }} " id="cartConnect">
                    </a>
                </li> 
                {% if not app.user %}
                <li>
                    <a href="{{ path('security_login') }}">
                        <i class="fas fa-sign-in-alt fa-sm"></i> Connexion
                    </a>
                </li>
                {% else %}
                <li>
                    <div class="row">
                    <a class="dropdown-trigger" data-target="dropdown1">
                            <img class="circle avatarNav" src="{{app.user.picture}}" alt="avatar de {{app.user.firstname}}">
                        
                        <div class="name">{{app.user.firstname}} </div>
                        
                        <i class="material-icons right">arrow_drop_down</i>
                    </a></div>
                </li>
                {% endif %}
            </ul>
        </div>
    </nav>
</div>